# ChoiceMind 界面设计契约

## 视觉论点

ChoiceMind 是一张正在展开的“决策航图”，不是通用聊天框或 SaaS 仪表盘。它服务于需要把消费需求、证据和结论逐层核验的中文用户；界面应像可靠、克制的研究工作台：信息密度可控，路径清楚，重要动作有分量。

## 设计源与适用范围

- 本文件是 ChoiceMind Web 的规范源；`apps/web/src/app/globals.css` 必须逐项映射这里的 Token。
- 新页面优先复用现有语义类和组件，不为单页另造视觉体系。
- 任何偏离都必须在 `UX-CONTRACT.md` 记录原因和回收计划。

## Token

| 类别 | 名称 | 值 | 用途 |
| --- | --- | --- | --- |
| 颜色 | 墨蓝 | `#17233b` | 主文字、主按钮、品牌锚点 |
| 颜色 | 铜橙 | `#c76532` | 当前路径、焦点、关键状态 |
| 颜色 | 纸白 | `#f5f1e8` | 页面底色 |
| 颜色 | 雾白 | `#fffdf8` | 表单与工作区表面 |
| 颜色 | 石灰 | `#6f746f` | 次级文字 |
| 颜色 | 线灰 | `#d8d2c5` | 分隔线、输入框边界 |
| 颜色 | 成功 | `#246b52` | 成功与已验证状态 |
| 颜色 | 危险 | `#a43e32` | 错误与不可逆警告 |
| 字体 | 正文 | `"Noto Sans SC", "Microsoft YaHei", sans-serif` | 中文正文与操作 |
| 字体 | 数据 | `"IBM Plex Mono", "Cascadia Mono", monospace` | 路径、编号、时间、代码 |
| 间距 | 基准 | `4px` | 所有间距采用 4px 倍数 |
| 圆角 | 小 | `6px` | 输入框、标签 |
| 圆角 | 中 | `12px` | 仅用于主要工作表面 |
| 动效 | 快 | `180ms` | 焦点、按钮、状态切换 |

## 排版与层级

- 只使用正文与数据两套字体；不远程加载字体。
- 页面标题采用紧凑字距，正文行高不低于 1.6。
- 说明文字不小于 14px，表单正文不小于 16px，避免浏览器移动端缩放。

## 布局

- 移动端单列优先，关键动作贴近当前内容，不使用固定底部遮挡。
- 桌面工作台采用非对称 `5:7` 节奏：左侧放任务脉络，右侧放证据与结论。
- 身份页使用“安静前厅”：左侧一句产品承诺与路径标记，右侧单一表单；不堆卡片。
- 管理页采用账册式表格和分隔线，不使用卡片网格。

## 标志性元素

“航迹线”由一条细线与离散节点组成，节点只表达真实阶段或状态。禁止把它当作无意义装饰，也禁止渐变、玻璃拟态和大面积阴影。

## 交互与无障碍

- 焦点轮廓使用铜橙色 3px 外圈，不能仅靠颜色表达错误。
- 微交互在 150–300ms 内；`prefers-reduced-motion` 下取消位移与平滑滚动。
- 所有表单都有显式 `label`、错误摘要和 `aria-live`；提交期间按钮禁用并保持可理解文案。
- 不可逆操作和 SUPERADMIN 高权限操作必须说明影响、要求重新认证，并在执行前提供明确确认。
- 停用、密码重置和邀请撤销等会立即改变用户状态的操作必须说明影响并提供明确确认；重新认证范围遵循 `#46` 冻结的权限规则。

## 禁止模式

- 不使用通用 SaaS 仪表盘、满屏圆角卡片、廉价渐变、无意义图标或装饰插画。
- 不把授权决定、角色声明、用户 ID、会话令牌或权限判断结果的生成交给客户端。客户端可以依据服务端返回的当前 Principal 隐藏无关入口，但 API 必须独立重复授权。
- 不通过占位骨架掩盖不可用状态；失败必须给出恢复动作。
